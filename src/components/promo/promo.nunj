{% from "../../components/button/button.nunj" import button %}
{% from "../../components/field/field.nunj" import field %}

{% macro promo() %}
    <div class="promo">
        <h3 class="title">{{ basketItem.promo.title }}</h3>
        <div class="promo__flex">
            {{ field(basketItem.promo.field) }}
            {{ button(basketItem.promo.btn) }}
        </div>
    </div>
{% endmacro %}